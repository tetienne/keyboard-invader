---
phase: 01-project-scaffolding-dev-tooling
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - .mise.toml
  - .npmrc
  - tsconfig.json
  - vite.config.ts
  - eslint.config.mjs
  - .prettierrc
  - .prettierignore
  - .editorconfig
  - src/style.css
  - src/shared/i18n/index.ts
  - src/shared/i18n/fr.json
  - src/shared/i18n/en.json
  - src/shared/types/index.ts
  - src/game/.gitkeep
  - src/screens/.gitkeep
autonomous: true
requirements:
  - DX-02
  - DX-03
  - DX-04
  - DX-05
must_haves:
  truths:
    - "pnpm install succeeds with zero errors"
    - "pnpm exec tsc --noEmit succeeds with strict mode"
    - "pnpm exec eslint . runs with strict+stylistic rules loaded"
    - "Tailwind CSS compiles via @tailwindcss/vite plugin"
    - "Path alias @/ resolves in both TypeScript and Vite"
    - "i18n t() function returns translation strings"
  artifacts:
    - path: "package.json"
      provides: "Project manifest with pnpm packageManager, all deps"
      contains: "packageManager"
    - path: "tsconfig.json"
      provides: "TypeScript strict config with path aliases"
      contains: "noUncheckedIndexedAccess"
    - path: "eslint.config.mjs"
      provides: "ESLint flat config with strict+stylistic"
      contains: "strictTypeChecked"
    - path: "vite.config.ts"
      provides: "Vite config with Tailwind plugin and path aliases"
      contains: "@tailwindcss/vite"
    - path: "src/shared/i18n/index.ts"
      provides: "i18n foundation with t() helper"
      exports: ["t", "setLocale", "getLocale"]
  key_links:
    - from: "tsconfig.json"
      to: "vite.config.ts"
      via: "path alias @/ defined in both"
      pattern: "@/"
    - from: "eslint.config.mjs"
      to: "tsconfig.json"
      via: "projectService reads tsconfig"
      pattern: "projectService.*true"
---

<objective>
Initialize the Keyboard Invader project with all configuration files: package.json with pnpm, TypeScript strict mode, Vite 8, ESLint strict flat config, Prettier, Tailwind CSS v4, path aliases, i18n foundation, and project directory structure.

Purpose: Establish the entire tooling foundation so subsequent plans can write application code and CI/CD against a working, strict dev environment.
Output: A fully configured project where `pnpm install && pnpm exec tsc --noEmit && pnpm exec eslint .` all pass.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-project-scaffolding-dev-tooling/01-CONTEXT.md
@.planning/phases/01-project-scaffolding-dev-tooling/01-RESEARCH.md
@.planning/phases/01-project-scaffolding-dev-tooling/01-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize project with pnpm, TypeScript, Vite, ESLint, Prettier, Tailwind, and all config files</name>
  <files>
    .mise.toml, .npmrc, package.json, tsconfig.json, vite.config.ts,
    eslint.config.mjs, .prettierrc, .prettierignore, .editorconfig,
    src/style.css
  </files>
  <read_first>
    .planning/phases/01-project-scaffolding-dev-tooling/01-RESEARCH.md (all config patterns, exact versions, exact file contents)
    .planning/phases/01-project-scaffolding-dev-tooling/01-UI-SPEC.md (Tailwind @theme block with design tokens)
    .planning/phases/01-project-scaffolding-dev-tooling/01-CONTEXT.md (locked decisions D-01 through D-18)
  </read_first>
  <action>
    **1. Create `.mise.toml`** (per D-05, D-06):
    ```toml
    [tools]
    node = "24.14.1"
    prek = "0.3.5"

    [hooks]
    postinstall = "corepack enable pnpm"

    [env]
    _.path = ["{{config_root}}/node_modules/.bin"]
    ```

    **2. Create `.npmrc`** (per D-07, pnpm strict resolution pitfall):
    ```ini
    auto-install-peers=true
    ```

    **3. Create `package.json`** via `pnpm init`, then edit to include:
    - `"name": "keyboard-invader"`
    - `"private": true`
    - `"type": "module"`
    - `"packageManager": "pnpm@10.33.0"` (exact version, corepack will add hash on first use)
    - Scripts:
      - `"dev": "vite"`
      - `"build": "tsc --noEmit && vite build"`
      - `"preview": "vite preview"`
      - `"lint": "eslint ."`
      - `"format": "prettier --write ."`
      - `"format:check": "prettier --check ."`
      - `"typecheck": "tsc --noEmit"`
      - `"test": "vitest run"`

    **4. Install dependencies** (exact versions from RESEARCH.md):
    ```bash
    pnpm add pixi.js@8.17.1
    pnpm add -D typescript@6.0.2 vite@8.0.3 @tailwindcss/vite@4.2.2 tailwindcss@4.2.2 vitest@4.1.2 eslint@10.1.0 prettier@3.8.1 typescript-eslint@8.57.2 @eslint/js@10.0.1 eslint-config-prettier@10.1.8 eslint-plugin-import-x@4.16.2 @commitlint/cli@20.5.0 @commitlint/config-conventional@20.5.0 wrangler@4.78.0
    ```

    **5. Create `tsconfig.json`** (per D-12, exact content from RESEARCH.md Pattern: TypeScript Strict Config):
    ```json
    {
      "compilerOptions": {
        "target": "ES2022",
        "module": "ESNext",
        "moduleResolution": "bundler",
        "strict": true,
        "noUncheckedIndexedAccess": true,
        "noImplicitOverride": true,
        "noPropertyAccessFromIndexSignature": true,
        "exactOptionalPropertyTypes": true,
        "forceConsistentCasingInFileNames": true,
        "isolatedModules": true,
        "verbatimModuleSyntax": true,
        "skipLibCheck": true,
        "declaration": true,
        "declarationMap": true,
        "sourceMap": true,
        "paths": {
          "@/*": ["./src/*"]
        },
        "outDir": "dist",
        "rootDir": "src"
      },
      "include": ["src/**/*.ts"],
      "exclude": ["node_modules", "dist"]
    }
    ```

    **6. Create `vite.config.ts`** (per D-04, D-13, RESEARCH.md Pattern 4):
    ```typescript
    import { defineConfig } from 'vite'
    import tailwindcss from '@tailwindcss/vite'
    import { resolve } from 'node:path'

    export default defineConfig({
      plugins: [tailwindcss()],
      resolve: {
        alias: {
          '@': resolve(__dirname, 'src'),
        },
      },
    })
    ```

    **7. Create `eslint.config.mjs`** (per D-10, RESEARCH.md Pattern 3):
    Exact content from RESEARCH.md: eslint.configs.recommended + tseslint.configs.strictTypeChecked + stylisticTypeChecked + importX + eslintConfigPrettier. Include `@typescript-eslint/no-explicit-any: 'error'`, `consistent-type-imports`, and `naming-convention` rules. Ignore `dist/`, `node_modules/`, `*.config.*`.

    **8. Create `.prettierrc`** (per D-11):
    ```json
    {
      "semi": false,
      "singleQuote": true,
      "tabWidth": 2,
      "printWidth": 100,
      "trailingComma": "all"
    }
    ```

    **9. Create `.prettierignore`**:
    ```
    dist
    node_modules
    pnpm-lock.yaml
    ```

    **10. Create `.editorconfig`** (per D-18, RESEARCH.md EditorConfig):
    ```ini
    root = true

    [*]
    indent_style = space
    indent_size = 2
    end_of_line = lf
    charset = utf-8
    trim_trailing_whitespace = true
    insert_final_newline = true

    [*.md]
    trim_trailing_whitespace = false
    ```

    **11. Create `src/style.css`** (per D-13, UI-SPEC @theme block):
    ```css
    @import 'tailwindcss';

    @theme {
      --color-dominant: #1a1a2e;
      --color-secondary: #16213e;
      --color-accent: #e94560;
      --color-destructive: #dc2626;
      --color-text-primary: #ffffff;
      --color-text-muted: #a0aec0;

      --spacing-xs: 4px;
      --spacing-sm: 8px;
      --spacing-md: 16px;
      --spacing-lg: 24px;
      --spacing-xl: 32px;
      --spacing-2xl: 48px;
      --spacing-3xl: 64px;

      --font-size-body: 16px;
      --font-size-label: 14px;
      --font-size-heading: 24px;
      --font-size-display: 48px;
    }
    ```

    **12. Create directory structure** (per D-03):
    - `src/game/.gitkeep`
    - `src/screens/.gitkeep`
    - `src/shared/types/index.ts` (empty export: `export {}`)
    - `public/` directory
  </action>
  <verify>
    <automated>pnpm install --frozen-lockfile 2>/dev/null || pnpm install && pnpm exec tsc --noEmit && pnpm exec eslint . && pnpm exec prettier --check .</automated>
  </verify>
  <done>All config files exist, pnpm install succeeds, TypeScript compiles in strict mode, ESLint runs with strict+stylistic rules, Prettier checks pass, path aliases resolve, Tailwind theme tokens declared.</done>
</task>

<task type="auto">
  <name>Task 2: Create i18n foundation with t() helper and locale files</name>
  <files>
    src/shared/i18n/index.ts, src/shared/i18n/fr.json, src/shared/i18n/en.json
  </files>
  <read_first>
    .planning/phases/01-project-scaffolding-dev-tooling/01-RESEARCH.md (i18n Foundation code example)
    .planning/phases/01-project-scaffolding-dev-tooling/01-UI-SPEC.md (Copywriting Contract -- exact i18n keys and values)
    .planning/phases/01-project-scaffolding-dev-tooling/01-CONTEXT.md (D-14: full i18n foundation)
    src/shared/types/index.ts (verify it exists from Task 1)
    eslint.config.mjs (naming-convention rules to comply with)
    tsconfig.json (verify paths alias works)
  </read_first>
  <action>
    **1. Create `src/shared/i18n/fr.json`** (per D-14, UI-SPEC copywriting contract):
    ```json
    {
      "app.title": "Keyboard Invader",
      "app.subtitle": "Apprends a taper en t'amusant !",
      "menu.play": "Jouer",
      "menu.profiles": "Profils",
      "menu.settings": "Reglages"
    }
    ```

    **2. Create `src/shared/i18n/en.json`** (per D-14, UI-SPEC copywriting contract):
    ```json
    {
      "app.title": "Keyboard Invader",
      "app.subtitle": "Learn to type while having fun!",
      "menu.play": "Play",
      "menu.profiles": "Profiles",
      "menu.settings": "Settings"
    }
    ```

    **3. Create `src/shared/i18n/index.ts`** (per D-14, RESEARCH.md i18n Foundation pattern):
    - Import fr.json and en.json with `import type` for the type, regular import for the value
    - Define `type Locale = 'fr' | 'en'`
    - Define `type TranslationKey = keyof typeof fr`
    - `const translations: Record<Locale, Record<string, string>> = { fr, en }`
    - `let currentLocale: Locale = detectLocale()`
    - `detectLocale()` checks `navigator.language.slice(0, 2)`, returns `'fr'` if French, `'en'` otherwise
    - Export `t(key: TranslationKey): string` -- returns `translations[currentLocale][key] ?? key`
    - Export `setLocale(locale: Locale): void`
    - Export `getLocale(): Locale`

    Ensure the code passes ESLint strict + naming-convention rules. Use `type` imports where appropriate per D-10 consistent-type-imports rule.
  </action>
  <verify>
    <automated>pnpm exec tsc --noEmit && pnpm exec eslint src/shared/i18n/</automated>
  </verify>
  <done>i18n module exports t(), setLocale(), getLocale(); fr.json and en.json contain all UI-SPEC placeholder keys; code passes TypeScript strict and ESLint strict checks.</done>
</task>

</tasks>

<verification>
1. `pnpm install` succeeds
2. `pnpm exec tsc --noEmit` exits 0 (TypeScript strict, DX-03)
3. `pnpm exec eslint .` exits 0 (ESLint strict, DX-02)
4. `pnpm exec prettier --check .` exits 0 (formatting)
5. `src/style.css` contains Tailwind @import and @theme tokens (DX-04)
6. `src/shared/i18n/index.ts` exports t, setLocale, getLocale (D-14)
</verification>

<success_criteria>
- All project config files exist and are valid
- TypeScript strict mode compiles without errors
- ESLint strict+stylistic rules pass on all source files
- Tailwind CSS v4 theme tokens declared
- i18n foundation ready with fr/en locale files
- Path alias @/ works in both TS and Vite
- Project directory structure matches D-03
</success_criteria>

<output>
After completion, create `.planning/phases/01-project-scaffolding-dev-tooling/01-01-SUMMARY.md`
</output>
