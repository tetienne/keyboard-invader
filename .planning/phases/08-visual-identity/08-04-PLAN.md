---
phase: 08-visual-identity
plan: 04
type: execute
wave: 4
depends_on: ["08-03"]
files_modified: []
autonomous: false
requirements:
  - AV-01
must_haves:
  truths:
    - "Falling letters/words have cartoon-styled alien containers (not plain text)"
    - "Consistent color palette and rounded, child-friendly visual language"
    - "Destruction effects (star particles) play when a letter/word is eliminated"
    - "All visual elements render without jank at 60fps on a mid-range laptop with 4x CPU throttle"
  artifacts: []
  key_links: []
---

<objective>
Visual verification checkpoint. Validate the complete visual identity works correctly in the browser: alien creatures, starfield, destruction effects, defender spaceship, SVG avatars, space-themed menus, and 60fps performance.

Purpose: Phase 8 success criteria require visual and performance validation that cannot be automated. This checkpoint ensures the space theme is cohesive and performant before marking the phase complete.

Output: User approval of visual identity, performance confirmation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/08-visual-identity/08-CONTEXT.md
@.planning/phases/08-visual-identity/08-UI-SPEC.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Visual identity and 60fps performance verification</name>
  <files></files>
  <action>
Present the complete visual identity for user verification. Run `pnpm dev` to start the dev server.

Verification checklist for the user:

1. **Profile screen:** SVG avatars display (3 free: 2 kids + 1 alien, 3 locked with lock overlay). Space-themed background with starfield.
2. **Menu screen:** Starfield background, space-themed mode buttons with glow borders, Fredoka rounded font.
3. **Letter mode gameplay:**
   - Letters appear inside cute alien creatures (round, blobby)
   - Aliens bob gently while falling (idle animation)
   - Pressing correct key: laser fires from spaceship, alien pops into star particles
   - Pressing wrong key: alien dodges/wobbles sideways
   - Alien reaching bottom: escapes with goodbye animation
   - Defender spaceship visible at bottom, subtle hover
   - Starfield parallax scrolls behind everything
   - Score and XP bar visible with glow styling
4. **Word mode gameplay:** Word aliens are visually larger/distinct from letter aliens
5. **Game over screen:** Space-themed results panel, level title displayed
6. **Level-up celebration:** Star-shaped particles instead of circles, brief warp speed effect
7. **Performance:** Chrome DevTools > Performance > CPU 4x slowdown > Record 30s gameplay > Sustained 60fps
8. **Font:** Text is sharp/crisp (not blurry), uses rounded Fredoka font (not Arial)
9. **Color:** Background is deep purple (not dark navy), letter colors are bright and harmonized
  </action>
  <verify>User confirms visual approval</verify>
  <done>User has approved the visual identity and confirmed 60fps performance, or has listed specific issues to address.</done>
</task>

</tasks>

<verification>
All Phase 8 success criteria from ROADMAP.md:
1. Falling letters/words have cartoon-styled containers or character decorations (not plain text)
2. The game uses a consistent color palette and rounded, child-friendly visual language
3. Destruction effects (particles, pops, sparkles) play when a letter/word is eliminated
4. All visual elements render without jank at 60fps on a mid-range laptop (validated with 4x CPU throttle)
</verification>

<success_criteria>
- User confirms visual identity is cohesive and appealing for children
- User confirms 60fps performance under 4x CPU throttle
- All 22 context decisions (D-01 through D-22) are visually implemented
</success_criteria>

<output>
After completion, create `.planning/phases/08-visual-identity/08-04-SUMMARY.md`
</output>
