# Phase 8.1: Critical Integration Fixes - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-04-12
**Phase:** 08.1-critical-integration-fixes
**Areas discussed:** Avatar ID migration, Word mode difficulty signal, Idle animation integration

---

## Avatar ID Migration

| Option | Description | Selected |
|--------|-------------|----------|
| Fix constant + migrate saved data (Recommended) | Update DEFAULT_UNLOCKED_AVATARS to new IDs AND add a schema migration (v2->v3) that runs migrateLegacyAvatarId() on existing profiles' avatarId + unlockedAvatarIds. Handles kids who already played. | |
| Fix constant only | Update DEFAULT_UNLOCKED_AVATARS to new IDs. Existing profiles with legacy IDs may show wrong avatars — acceptable if no real users yet. | ✓ |
| You decide | Claude chooses the best approach based on codebase state | |

**User's choice:** Fix constant only
**Notes:** No real users exist yet, so schema migration is unnecessary overhead.

---

## Word Mode Difficulty Signal

| Option | Description | Selected |
|--------|-------------|----------|
| Add recordResult(false) on wrong key (Recommended) | Mirror the letter mode pattern — each wrong key press counts as a miss for difficulty adjustment. Simple and consistent. | ✓ |
| Add recordResult(false) + per-char hits | Also call recordResult(true) on each correct character (not just word completion). Gives the difficulty system more signal for word mode. | |
| You decide | Claude chooses what makes sense for the adaptive difficulty window | |

**User's choice:** Add recordResult(false) on wrong key
**Notes:** Keep it consistent with letter mode — one recordResult(false) per wrong key.

---

## Idle Animation Integration

| Option | Description | Selected |
|--------|-------------|----------|
| Call for all active entities (Recommended) | Iterate both activeEntities and activeWordEntities, call updateIdle(dt) on each container. All falling aliens bob and blink uniformly. | ✓ |
| Only non-tweening entities | Skip updateIdle() for entities currently running a hit/miss/escape tween to avoid visual conflicts. | |
| You decide | Claude determines the best placement based on the tween system behavior | |

**User's choice:** Call for all active entities
**Notes:** All aliens animate uniformly. Tween system and idle system affect different properties.

---

## Claude's Discretion

- Exact placement of updateIdle() call within the update loop
- Whether to update existing unit tests or add new ones
- Minor code cleanup directly related to the three fixes

## Deferred Ideas

None — discussion stayed within phase scope
