---
phase: 08.3-dx-dead-code-cleanup
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - vite.config.ts
autonomous: true
requirements:
  - DX-01
must_haves:
  truths:
    - "Fresh clones running `prek install --hook-type pre-commit --hook-type commit-msg` install BOTH hooks (per the README instruction)"
    - "A non-conventional commit message (e.g., 'bad message') is rejected end-to-end by the local commit-msg hook"
    - "vite.config.ts test.exclude already lists '.claude/**' (verified, no change required)"
  artifacts:
    - path: "README.md"
      provides: "Updated install command on line 16 — `prek install --hook-type pre-commit --hook-type commit-msg`"
      contains: "--hook-type commit-msg"
    - path: ".git/hooks/commit-msg"
      provides: "Locally installed commit-msg hook (file exists after running the new install command). Not committed; per-clone artifact."
    - path: "vite.config.ts"
      provides: "Already excludes .claude/** — verified, no edit performed in this plan"
      contains: "'.claude/**'"
  key_links:
    - from: "README.md install line"
      to: ".git/hooks/commit-msg"
      via: "prek install --hook-type commit-msg writes the hook on a fresh clone"
      pattern: "--hook-type commit-msg"
    - from: ".git/hooks/commit-msg"
      to: "commitlint.config.ts"
      via: "prek.toml declares `entry = 'pnpm exec commitlint --edit'` for stage commit-msg"
      pattern: "stages = \\[\"commit-msg\"\\]"
---

<objective>
Close DX-01: propagate the missing `commit-msg` hook to fresh clones by fixing the README
install instruction; verify locally end-to-end that a non-conventional message is rejected;
and verify (no edit) that `vite.config.ts` already excludes `.claude/**`.

Purpose: The audit identified that `.git/hooks/commit-msg` does not exist after a fresh
`prek install`. The hook itself (commitlint) is correctly declared in prek.toml — only the
README install command needs the `--hook-type commit-msg` flag.
Output: README install line updated; local hook installed and verified by a rejected commit;
vite.config.ts confirmation logged.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08.3-dx-dead-code-cleanup/08.3-CONTEXT.md
@.planning/v1.0-MILESTONE-AUDIT.md

@README.md
@vite.config.ts
@prek.toml
@commitlint.config.ts

<interfaces>
<!-- The relevant config files. Executor should NOT change prek.toml or commitlint.config.ts. -->

From prek.toml (already correct, do NOT edit):
```toml
[[repos.hooks]]
id = "commitlint"
name = "commitlint"
language = "system"
entry = "pnpm exec commitlint --edit"
stages = ["commit-msg"]
```

From README.md (current line 16 — to update):
```
prek install          # Sets up git hooks
```

After update (D-04):
```
prek install --hook-type pre-commit --hook-type commit-msg          # Sets up git hooks
```

From vite.config.ts (current state — verify, do NOT edit):
```typescript
test: {
  exclude: ['.claude/**', 'node_modules/**'],
},
```

Per D-14 — `.claude/**` already present. The verification task only `grep`s to confirm and writes nothing.

Per D-06 — Do NOT add a CI-level commitlint job. The local commit-msg hook + correct README satisfies the "validates on every commit" SC.

Per D-15 — Do not investigate or edit other potential install-command surfaces unless found. Per Claude's discretion in CONTEXT.md, check briefly for `CONTRIBUTING.md` or mise tasks; if none, README-only is sufficient. (Verified at planning time: no `CONTRIBUTING.md`, `.mise.toml` only declares tools, no install task.)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update README install command, install commit-msg hook locally, and verify end-to-end rejection of a bad commit message</name>
  <files>README.md</files>
  <read_first>
    - README.md (lines 1-25 — Setup section, locate the `prek install` line on line 16)
    - prek.toml (confirm the `commitlint` hook is declared with `stages = ["commit-msg"]` — read once, no edit)
    - commitlint.config.ts (confirm config resolves — read once, no edit)
  </read_first>
  <action>
**Per D-04 — Update `README.md` line 16.**

Change line 16:

FROM:
```
prek install          # Sets up git hooks
```

TO:
```
prek install --hook-type pre-commit --hook-type commit-msg   # Sets up git hooks
```

The two extra spaces in the original `prek install          # Sets up git hooks` are no longer needed for column alignment after the longer command — use a single readable single-space gap before `#`. Concretely, the new line should be exactly:
`prek install --hook-type pre-commit --hook-type commit-msg   # Sets up git hooks`
(Three spaces before `#` are fine; preserve the surrounding code-fence formatting.)

**Sanity-check there are no other install-command surfaces to update:**

Run these checks. If a match is found in either, update it the same way; if not, README is the only surface and this is fine.

```bash
grep -rn "prek install" --include="*.md" --include="*.toml" --include="*.json" --include="*.yaml" --include="*.yml" .
# Expected: only README.md:16
```

```bash
ls CONTRIBUTING.md CONTRIBUTING.MD 2>/dev/null
# Expected: no matches
```

If the first command finds additional locations (e.g., a CONTRIBUTING.md added between planning and execution), update each occurrence to the same `prek install --hook-type pre-commit --hook-type commit-msg` form. Otherwise proceed.

**Per D-05 — Install the hook locally and verify end-to-end rejection.**

Run the new install command (this creates `.git/hooks/commit-msg` if missing):

```bash
prek install --hook-type pre-commit --hook-type commit-msg
```

Confirm both hook files now exist:

```bash
test -f .git/hooks/pre-commit && test -f .git/hooks/commit-msg && echo "BOTH HOOKS PRESENT"
```

Now perform an end-to-end rejection test. Stage a trivial whitespace-only change (or use `git commit --allow-empty`) and attempt a commit with a non-conventional message:

```bash
git commit --allow-empty -m "bad message" 2>&1 | tee /tmp/commit-msg-test.log
echo "EXIT: $?"
```

The commit MUST be rejected (non-zero exit). The output must contain commitlint error markers — at minimum the substring `subject may not be empty` OR `type may not be empty` OR `commitlint` — proving the hook ran and rejected the message. If the commit succeeds, the hook is not actually wired and the task fails.

After verifying rejection, run a sanity-check positive case to make sure conventional messages still pass:

```bash
git commit --allow-empty -m "chore(08.3): verify commit-msg hook" --dry-run 2>&1 || true
```

(`--dry-run` does not actually commit but does run the hook; the exit must be 0.)

Capture the rejection log path (`/tmp/commit-msg-test.log`) in the SUMMARY for evidence.

**Do NOT modify** `prek.toml`, `commitlint.config.ts`, `.git/hooks/*` directly, or any CI workflow file. Per D-06, no CI commitlint job is added in this phase.
  </action>
  <verify>
    <automated>grep -nE "^prek install --hook-type pre-commit --hook-type commit-msg" README.md && test -f .git/hooks/commit-msg && (git commit --allow-empty -m "bad message" 2>&1 | grep -qE "commitlint|subject may not be empty|type may not be empty" && echo "REJECTION VERIFIED")</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "prek install --hook-type pre-commit --hook-type commit-msg" README.md` returns 1 match
    - `grep -n "^prek install\s*#" README.md` returns no matches (old form removed)
    - `test -f .git/hooks/commit-msg` exits 0 (hook installed locally)
    - `test -f .git/hooks/pre-commit` exits 0 (existing hook still present)
    - Running `git commit --allow-empty -m "bad message"` exits non-zero AND the captured output contains one of `commitlint` / `subject may not be empty` / `type may not be empty`
    - Running `git commit --allow-empty -m "chore(08.3): test" --dry-run` exits 0 (conventional message passes the hook)
    - `prek.toml` is unchanged (no diff on this file)
    - `commitlint.config.ts` is unchanged (no diff on this file)
    - No CI workflow file modified
  </acceptance_criteria>
  <done>README line 16 instructs fresh clones to install both hook types. The local maintainer's `.git/hooks/commit-msg` is in place. A non-conventional message is rejected end-to-end with commitlint output in the failure log. Conventional messages pass. No unrelated configs or CI workflows touched.</done>
</task>

<task type="auto">
  <name>Task 2: Verify vite.config.ts already excludes .claude/** (no edit required)</name>
  <files>vite.config.ts</files>
  <read_first>
    - vite.config.ts (full file — 17 lines)
  </read_first>
  <action>
**Per D-14 — Verify-only, no edit.**

Read `vite.config.ts` and confirm the `test.exclude` array contains `'.claude/**'`. The current file (verified at planning time) is:

```typescript
/// <reference types="vitest/config" />
import { defineConfig } from 'vite'
import tailwindcss from '@tailwindcss/vite'
import { resolve } from 'node:path'

export default defineConfig({
  plugins: [tailwindcss()],
  resolve: {
    alias: {
      '@': resolve(__dirname, 'src'),
    },
  },
  test: {
    exclude: ['.claude/**', 'node_modules/**'],
  },
})
```

Run the verification check:

```bash
grep -n "'\.claude/\*\*'" vite.config.ts
```

This must return exactly 1 match on line 14 (or wherever the `exclude` array is). If the match is missing, the config drifted between planning and execution — in that case, restore the array to `['.claude/**', 'node_modules/**']`. Otherwise, do nothing (no commit, no diff).

If the verification confirms the exclude is already in place, record that fact in the plan SUMMARY. Do NOT modify `vite.config.ts` if `.claude/**` is already present — per D-14 this is verify-only.
  </action>
  <verify>
    <automated>grep -nE "'\.claude/\*\*'" vite.config.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "'\.claude/\*\*'" vite.config.ts` returns 1 match
    - `git diff vite.config.ts` shows no changes (file untouched per D-14) — UNLESS the planning-time observation was wrong, in which case the executor restores the exclude entry and notes the unexpected drift in the SUMMARY
    - `pnpm exec vitest run` does not pick up any test files under `.claude/` (sanity)
  </acceptance_criteria>
  <done>`vite.config.ts` `test.exclude` confirmed to contain `'.claude/**'`. No edit performed. SC bullet 4's vitest-config half is satisfied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None new | Documentation update + local hook install + verify-only config check. No new code, no new attack surface, no external input handled. The `git commit --allow-empty -m "bad message"` invocation is a controlled local test against an empty commit and produces no remote-visible artifact. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08.3-06 | T (Tampering) | README install command | accept | Public documentation; no privileged escalation. Adding `--hook-type commit-msg` causes prek to install a hook that runs `pnpm exec commitlint --edit` on commit — same trust level as the existing pre-commit hooks. Severity: NONE |
| T-08.3-07 | E (Elevation of Privilege) | Local commit-msg hook | accept | Hook runs `pnpm exec commitlint` against the user-authored commit message in the user's own working tree. No new privilege boundary; commitlint is already a declared dev-dependency vetted at Phase 1. Severity: NONE |
</threat_model>

<verification>
1. `grep -n "prek install --hook-type pre-commit --hook-type commit-msg" README.md` — 1 match
2. `test -f .git/hooks/commit-msg && test -f .git/hooks/pre-commit` — both exist
3. End-to-end rejection: `git commit --allow-empty -m "bad message"` exits non-zero and output contains `commitlint`/`subject may not be empty`/`type may not be empty`
4. Sanity positive: `git commit --allow-empty -m "chore(08.3): test" --dry-run` exits 0
5. `grep -n "'\.claude/\*\*'" vite.config.ts` — 1 match (D-14 verified)
6. `git diff prek.toml commitlint.config.ts vite.config.ts` — no changes
</verification>

<success_criteria>
- README line 16 carries the fix that propagates commit-msg hook to fresh clones
- The local commit-msg hook is installed and proven to reject non-conventional messages end-to-end
- vite.config.ts confirmed to already exclude `.claude/**` (no edit, just verify)
- No CI commitlint job introduced (per D-06)
- No unrelated config files modified
</success_criteria>

<output>
After completion, create `.planning/phases/08.3-dx-dead-code-cleanup/08.3-03-SUMMARY.md`
</output>
