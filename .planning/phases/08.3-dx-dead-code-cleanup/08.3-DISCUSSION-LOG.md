# Phase 8.3: DX & Dead Code Cleanup - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-04-26
**Phase:** 08.3-dx-dead-code-cleanup
**Areas discussed:** Pause architecture, commit-msg hook setup, Cleanup boundary / scope, Dead i18n key sweep depth

---

## Pause architecture

### Q1: How should we handle the dead PausedState?

| Option | Description | Selected |
|--------|-------------|----------|
| Delete dead state (Recommended) | Remove PausedState class (states.ts:1253), import (game.ts:20, index.ts:7), and registration (game.ts:85). Pause keeps using existing _pause()/_pauseOverlay path. Smallest blast radius. | ✓ |
| Rewire pause through FSM | Keep PausedState, refactor _pause()/_resume() to call stateMachine.transitionTo('paused'). Architecturally correct but expands scope. | |

**User's choice:** Delete dead state
**Notes:** Matches phase scope ("dead code removal") and 8.1/8.2 surgical-fix pattern.

### Q2: Should we remove the unused _pauseOverlay machinery if we keep pause in game.ts?

| Option | Description | Selected |
|--------|-------------|----------|
| Leave overlay code as-is (Recommended) | _pauseOverlay is used by the working _pause()/_resume() flow — not dead. | ✓ |
| Audit overlay code too | Re-verify _pauseOverlay path is wired; remove dead branches if any. | |

**User's choice:** Leave overlay code as-is
**Notes:** Confirmed by codebase scout — _pauseOverlay has 8+ live references in game.ts.

---

## commit-msg hook setup

### Q1: How should we ensure commit-msg installs for fresh clones?

| Option | Description | Selected |
|--------|-------------|----------|
| Update README + verify locally (Recommended) | Change README:16 to `prek install --hook-type pre-commit --hook-type commit-msg`. Verify both hooks exist and a bad commit is rejected. | ✓ |
| Add a pnpm postinstall script | Add `"postinstall": "prek install ..."` to package.json so hooks install automatically. More automatic but adds install side-effect. | |
| Verify only | Confirm hook exists locally with bad-commit test, leave README alone. Risks fresh clones missing the hook. | |

**User's choice:** Update README + verify locally
**Notes:** Documentation fix is the right surface area for this gap.

### Q2: Should we add a CI check that commitlint runs on PR commits?

| Option | Description | Selected |
|--------|-------------|----------|
| No — out of scope (Recommended) | SC says "validates on every commit" — local hook + README satisfies. CI commitlint is a separate concern. | ✓ |
| Yes, add commitlint to CI | Add a CI job for commitlint over PR commit range. Belt-and-suspenders, expands scope. | |

**User's choice:** No — out of scope

---

## Cleanup boundary / scope

### Q1: Strict scope, or fold nearby tech-debt items from the audit?

| Option | Description | Selected |
|--------|-------------|----------|
| Strict to 4 SCs (Recommended) | Only the 4 success criteria. Audit's other tech-debt deferred. | ✓ |
| Fold cheap incidentals | Add the 1-line stale-comment removal at states.ts:992. Skip MenuState i18n hardcoding and DestructionEffect. | |
| Full audit cleanup pass | Address all tech-debt: stale comment, MenuState i18n, DestructionEffect. Largest scope. | |

**User's choice:** Strict to 4 SCs
**Notes:** Mirrors surgical approach of 8.1/8.2.

### Q2: If new dead code is found beyond the audit list while doing the cleanup, what should we do?

| Option | Description | Selected |
|--------|-------------|----------|
| Note + defer (Recommended) | Capture in CONTEXT.md deferred or todos. Keep change set reviewable. | ✓ |
| Remove if trivially safe | Single-file, no callers, no test coverage → remove in same commit. Bigger items deferred. | |
| Always include in this phase | Maximum cleanup, less predictable diff. | |

**User's choice:** Note + defer

---

## Dead i18n key sweep depth

### Q1: How deep should the i18n key sweep go?

| Option | Description | Selected |
|--------|-------------|----------|
| Audit-listed 6 only (Recommended) | Remove the 6 flagged keys from en.json + fr.json. Matches SC wording. | ✓ |
| Full sweep + safety check | Grep every key against src/, remove zero-usage keys. Risk: false positives for dynamic keys. | |
| Full sweep, but list before deleting | Generate unused key list, review, then delete confirmed ones. Safer but more work. | |

**User's choice:** Audit-listed 6 only

### Q2: After the sweep, do you want to add any safeguard to prevent future dead keys?

| Option | Description | Selected |
|--------|-------------|----------|
| No safeguard (Recommended) | Future audits / tooling phase handles this. | |
| Add a unit test | Vitest reads en.json keys and asserts each appears at least once in src/. Catches dead keys on next CI run. | ✓ |
| Add to a future tooling phase | Defer to dedicated DX/tooling phase. | |

**User's choice:** Add a unit test
**Notes:** User picked the non-recommended option — they want a regression guard in place now rather than later.

---

## Claude's Discretion

- Exact placement / filename of the new i18n usage test
- Grep technique inside the test (Node fs walk vs ripgrep spawn)
- Whether to update install command in additional places beyond README:16 (CONTRIBUTING.md, mise tasks if found)
- Commit granularity for the cleanup (one big commit vs split by concern)

## Deferred Ideas

- Stale comment at `src/game/states.ts:992` (`placeholder: 2500ms pause`)
- Hardcoded i18n strings in `MenuState` (Phase 4 tech-debt)
- `DestructionEffect` circle Graphics instead of star sprites (accepted deviation)
- Full sweep of unused i18n keys beyond the audit list
- Any new dead code discovered mid-execution
