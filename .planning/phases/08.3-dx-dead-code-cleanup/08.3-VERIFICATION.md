---
phase: 08.3-dx-dead-code-cleanup
verified: 2026-04-26T20:08:00Z
status: passed
score: 4/4 must-haves verified
overrides_applied: 0
---

# Phase 08.3: DX & Dead Code Cleanup Verification Report

**Phase Goal:** Install missing commit-msg hook and remove all dead code identified by milestone audit.
**Verified:** 2026-04-26T20:08:00Z
**Status:** passed
**Re-verification:** No (initial verification)

## Goal Achievement

### Observable Truths

| #   | Truth (Roadmap Success Criterion) | Status | Evidence |
| --- | --------------------------------- | ------ | -------- |
| 1   | commit-msg hook installed and commitlint validates commit messages on every commit | VERIFIED | README.md:16 carries `prek install --hook-type pre-commit --hook-type commit-msg`; `.git/hooks/commit-msg` exists (prek-generated, ID 182c10f1...); bad commit `git commit --allow-empty -m "broken commit message"` rejected with exit 1 and commitlint output (`subject may not be empty [subject-empty]`, `type may not be empty [type-empty]`); positive case `chore(08.3): ...` accepted (test commit created and reset) |
| 2   | PausedState dead code removed from state machine | VERIFIED | `grep -rn "PausedState" src/ tests/` returns 0 matches; `'paused'` absent from `StateName` union (src/game/types.ts:18-23) and from `TRANSITIONS` map (src/game/types.ts:26-31); live pause flow `_pause/_resume/_unpause/_pauseOverlay` preserved in src/game/game.ts (19 occurrences across lines 44-292); invalid-transition test (tests/game/states.test.ts:66-73) uses `'gameover'` instead of `'paused'` |
| 3   | Dead exports (createBottomTween, createMissTween, resolveLevel, preallocatePools) removed | VERIFIED | `grep -rn "createMissTween\|createBottomTween" src/ tests/` returns 0 matches; `grep -rn "preallocatePools" src/ tests/` returns 0 matches; `resolveLevel` demoted to module-private (src/game/progression.ts:52 has `function resolveLevel`, no `export`), still called internally at line 70 (`Math.min(resolveLevel(newTotalXp), MAX_LEVEL)`); barrel `src/game/index.ts` no longer re-exports any removed symbol |
| 4   | Dead i18n keys removed and vitest config excludes .claude/** files | VERIFIED | All 6 audit-listed keys absent from both en.json and fr.json (`app.title`, `app.subtitle`, `boot.loading`, `menu.play`, `menu.profiles`, `menu.settings`); 9 surfaced orphans also absent (`progression.xp.earned`, `progression.avatar.unlocked`, `boot.error`, `levelTitle.{cadet,apprenti,pilote,capitaine,commandant,amiral}`); substring-fix orphan `progression.level` also absent (deleted in commit `d8f8aa5`); en/fr key parity 15/15; `tests/i18n/keys-used.test.ts` exists with quote-anchored matching (post WR-01 fix in `1f03962`) and passes; `vite.config.ts:14` `test.exclude` array contains `'.claude/**'` |

**Score:** 4/4 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
| -------- | -------- | ------ | ------- |
| `README.md` | Install command includes `--hook-type commit-msg` | VERIFIED | Line 16: `prek install --hook-type pre-commit --hook-type commit-msg   # Sets up git hooks` |
| `.git/hooks/commit-msg` | Per-clone hook present on maintainer's machine | VERIFIED | File exists (449 bytes); generated by prek with ID `182c10f181da4464a3eec51b83331688`; invokes `prek hook-impl --hook-type=commit-msg` |
| `prek.toml` | Declares commitlint at commit-msg stage | VERIFIED | Hook block: `id = "commitlint"`, `entry = "pnpm exec commitlint --edit"`, `stages = ["commit-msg"]` |
| `commitlint.config.ts` | Config file resolves | VERIFIED | File exists at repo root, 67 bytes |
| `src/game/states.ts` | `PausedState` class deleted | VERIFIED | 0 grep matches |
| `src/game/game.ts` | `PausedState` import + registration + `preallocatePools` method gone; live pause flow preserved | VERIFIED | 0 matches for `PausedState`, `paused: new`, `preallocatePools`; 19 matches for `_pause/_resume/_unpause/_pauseOverlay` |
| `src/game/types.ts` | `'paused'` removed from `StateName` and `TRANSITIONS`; `preallocatePools(): void` removed from `GameContext` | VERIFIED | StateName union: boot \| profiles \| menu \| playing \| gameover; TRANSITIONS keyed by same set; 0 matches for `preallocatePools` |
| `src/game/index.ts` | `PausedState`, `createMissTween`, `createBottomTween` re-exports removed | VERIFIED | 0 grep matches for any of these symbols |
| `src/game/tween.ts` | `createMissTween` and `createBottomTween` deleted | VERIFIED | 0 grep matches |
| `src/game/progression.ts` | `resolveLevel` demoted to module-private; internal call by `applyXp` preserved | VERIFIED | Line 52: `function resolveLevel(totalXp: number): number {` (no `export`); line 70: `Math.min(resolveLevel(newTotalXp), MAX_LEVEL)` preserved |
| `src/shared/i18n/en.json` | 6 audit-listed dead keys + 9 surfaced orphans + `progression.level` removed | VERIFIED | 15 keys remain; all listed dead keys absent; valid JSON; parity with fr.json |
| `src/shared/i18n/fr.json` | Mirrors en.json key set | VERIFIED | 15 keys; `Object.keys(en).sort() === Object.keys(fr).sort()` returns `true` |
| `tests/i18n/keys-used.test.ts` | Regression-guard test exists with quote-anchored matching | VERIFIED | File exists, 51 lines; uses `[\`'${key}'\`, \`"${key}"\`, \`\`${key}\`\`]` quote-anchored matching (WR-01 fix); test passes |
| `vite.config.ts` | `test.exclude` contains `.claude/**` | VERIFIED | Line 14: `exclude: ['.claude/**', 'node_modules/**']` |
| `tests/smoke.test.ts` | Stale dead-key describe block removed; 4 other describes preserved | VERIFIED | 0 matches for "i18n locale files exist and have required keys" or "requiredKeys"; 4 matches for the 4 preserved describe titles |
| `tests/game/states.test.ts` | `PausedState` ref + `preallocatePools` mock removed; invalid-transition test target swapped to `'gameover'` | VERIFIED | 0 matches for PausedState/preallocatePools/'paused'; line 72 calls `sm.transition('gameover', ctx)` |
| `tests/game/tween.test.ts` | `createMissTween`/`createBottomTween` imports + describes removed | VERIFIED | 0 grep matches for those symbols |
| `tests/game/progression.test.ts` | `resolveLevel` import + describe removed | VERIFIED | 0 grep matches |
| `tests/game/profile-state.test.ts` | `preallocatePools` mock entry removed | VERIFIED | 0 grep matches |

### Key Link Verification

| From | To | Via | Status | Details |
| ---- | -- | --- | ------ | ------- |
| README.md install line | .git/hooks/commit-msg | `prek install --hook-type commit-msg` | WIRED | README line 16 carries `--hook-type commit-msg`; hook physically installed; end-to-end rejection proven |
| .git/hooks/commit-msg | commitlint.config.ts | prek.toml declares `stages = ["commit-msg"]` for commitlint | WIRED | prek.toml block confirmed; bad commit produces commitlint output naming `subject-empty` and `type-empty` rules from `@commitlint/config-conventional` |
| StateMachine constructor | StateName union | `Record<StateName, GameState>` exhaustive — 'paused' removed from BOTH together | WIRED | StateName has 5 entries (boot, profiles, menu, playing, gameover); StateMachine registers exactly those 5; tsc passes (exhaustive) |
| applyXp | resolveLevel (module-private) | applyXp calls resolveLevel internally; demotion preserves call site | WIRED | progression.ts:70 still calls `resolveLevel(newTotalXp)`; tsc passes; tests cover threshold transitions transitively (40+30 → level 2; 1900+100 → cap at 10) |
| tests/i18n/keys-used.test.ts | src/shared/i18n/en.json | `JSON.parse(readFileSync(en.json))` drives iteration | WIRED | Test file confirmed at lines 7, 31-32 |
| tests/i18n/keys-used.test.ts | src/ source tree | Recursive walk + quote-anchored scan; excludes both en.json and fr.json | WIRED | EXCLUDED_FILES at line 10 contains both paths; quote-anchored matching at line 41-42 (post WR-01 fix) |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
| -------- | ------- | ------ | ------ |
| Type check passes | `pnpm exec tsc --noEmit` | exit 0, no output | PASS |
| Full test suite passes | `pnpm exec vitest run` | 21 files, 234 tests, all pass, duration 732ms | PASS |
| keys-used safeguard test passes | `pnpm exec vitest run tests/i18n/keys-used.test.ts` | 1 test passes | PASS |
| Bad commit rejected | `git commit --allow-empty -m "broken commit message"` | Exit 1, commitlint output: `subject may not be empty`, `type may not be empty` | PASS |
| Conventional commit accepted | `git commit --allow-empty -m "chore(08.3): verification positive case test"` (cleaned with `git reset --hard HEAD~1`) | Exit 0; commit `d93dcb1` created and reverted | PASS |
| ESLint on new safeguard test | `pnpm exec eslint tests/i18n/keys-used.test.ts --max-warnings=0` | exit 0 | PASS |
| ESLint full surface | `pnpm exec eslint src tests --max-warnings=0 --no-warn-ignored` | exit 1 — 82 errors, 0 warnings (unchanged from base commit `967fc39`, documented in Plan 01 SUMMARY as out-of-scope per D-15/D-16) | ACCEPTED (pre-existing baseline, not regression) |
| i18n key parity | `node -e "..."` comparing Object.keys | true; 15/15 | PASS |
| vitest does not pick up `.claude/**` | vitest run | 21 test files (all under `tests/`), no `.claude/` files included | PASS |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
| ----------- | ----------- | ----------- | ------ | -------- |
| DX-01 | 08.3-03 | Pre-commit hooks (pre-commit framework) valident le code avant chaque commit | SATISFIED | README install command propagates BOTH hooks; commit-msg hook installed via prek; commitlint declared at `stages = ["commit-msg"]`; end-to-end rejection of non-conventional commit verified locally (exit 1 + commitlint output); positive case for conventional commit verified |

REQUIREMENTS.md still lists DX-01 with `[ ]` and Status `Pending` for "Phase 1, 8.3" — this is a documentation lag (not a code gap). The implementation is satisfied; only the checklist mark in REQUIREMENTS.md has not been ticked. Recommend the orchestrator update the REQUIREMENTS.md status as a documentation follow-up; this does not block phase pass since the codebase contract is met.

### Anti-Patterns Found

None in modified files.

| File | Line | Pattern | Severity | Impact |
| ---- | ---- | ------- | -------- | ------ |
| (none) | -- | -- | -- | -- |

`grep -nE "TODO|FIXME|XXX|HACK|PLACEHOLDER"` over the 6 modified source files (`src/game/{states,game,types,index,tween,progression}.ts`) returned 0 matches.

### Human Verification Required

None — all success criteria verified programmatically. The end-to-end commit-msg hook test was executed live against the maintainer's working clone and produced both the expected rejection (bad message) and the expected acceptance (conventional message).

Per-clone caveat: `.git/hooks/commit-msg` is a per-clone artifact (not committed). Fresh clones are guaranteed to install the hook only if they follow the README line 16 instruction. The README correctly carries the `--hook-type commit-msg` flag, so no propagation gap remains for new contributors.

### Gaps Summary

No gaps. All 4 roadmap success criteria are satisfied in the live codebase. Quality gates: tsc=0, vitest=234/234 pass, keys-used safeguard=pass, end-to-end commitlint rejection=verified, conventional acceptance=verified, key parity=15/15.

The pre-existing 82 ESLint errors are explicitly documented as out-of-scope per Plan 01 SUMMARY (`D-15/D-16`, baseline at commit `967fc39` matches current count file-for-file) and are not a regression from this phase.

The post-review WR-01 fix (substring matching → quote-anchored matching) is already applied in commit `1f03962 fix(08.3-04): tighten i18n safeguard to quote-anchored matching`, and the surfaced `progression.level` orphan is already deleted in commit `d8f8aa5 chore(08.3-04): delete 9 orphan i18n keys surfaced by safeguard test`. The IN-02 stale comment finding (`// State machine types (D-04, D-05)` in types.ts:17) is documentation drift, info-only, and does not block phase verification.

---

_Verified: 2026-04-26T20:08:00Z_
_Verifier: Claude (gsd-verifier)_
